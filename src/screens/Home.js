import React from 'react';
import {View,Text} from 'react-native';

const Home = (Props)=>{

    return (
        <View>
            <Text>{Props.userName}</Text>
        </View>
    )
};
 export default Home
